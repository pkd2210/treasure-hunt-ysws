export type Item = {
    id: string;
    name: string;
    description: string;
    price: number;
    imageUrl: string;
};

export type User = {
    slackId: string;
    name: string;
    role: string;
};
